import numpy as np
import os
import matplotlib.pyplot as plt

class FormationManager:
    def __init__(self, formation_files, num_clusters, algorithm):
        self.files = formation_files
        self.K = num_clusters
        self.algorithm = algorithm

    def run(self):
        algorithm.run(asdf)

    def visualize(self):
        algorithm.visualize(asdf)

    def summarize(self):
        algorithm.summarize(asdf)